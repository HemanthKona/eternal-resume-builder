import React from 'react';

/* eslint-disable-next-line */
export interface BasicProps {}

export const Basic = (props: BasicProps) => {
  return (
    <div>
      <h1>Welcome to basic component!</h1>
    </div>
  );
};

export default Basic;
